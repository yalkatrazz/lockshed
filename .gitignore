# Build output - regenerate with build.bat, don't commit
/dist/
/dist_installer/
/build/work/
__pycache__/
*.pyc

# Internal build tooling (PyInstaller spec, Inno Setup script, build.bat) -
# kept local only, not part of the public repo. Regenerate/recreate these
# from your own backup if you ever need them again on a fresh clone.
/build/
/build.bat

# User-specific data (never relevant to the repo)
*.enc
*.lockshed_settings.json
